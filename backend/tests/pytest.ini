[pytest]
# Maintain the existing cache directory setting
cache_dir = .\test_output

# Add async test configuration
asyncio_mode = auto


# Optional but recommended: Add explicit marker registration
markers =
    asyncio: mark test as async (pytest-asyncio)